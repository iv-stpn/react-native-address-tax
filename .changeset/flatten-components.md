---
"react-native-address-tax": patch
---

Flatten the components directory by inlining component files directly under `src/components/` instead of nesting each in its own subfolder. Internal-only change; the public API is unchanged.
